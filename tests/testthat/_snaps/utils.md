# It aborts when `value` has more than one element

    Code
      getIndexClosestToValue(value = value, array = array, thresholdRel = 0.1)
    Condition
      Error in `ospsuite.utils::validateIsOfLength()`:
      ! Object should be of length 1, but is of length 2 instead.

