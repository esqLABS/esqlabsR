# Refactoring log

One bullet per commit, newest at the bottom: `- YYYY-MM-DD HH:MM: plain-language one-liner`. See the "Log every commit" rule in `REFACTORING.md`.

- 2026-07-22 11:08: Added the refactoring brief (goal, rules, codebase conventions) and this commit log; renamed leftover "entity" wording to "definition" in a few comments.
- 2026-07-22 12:05: Documented that a script observed-data source runs arbitrary R (security note on loadObservedData and in the vignette).
- 2026-07-22 12:10: Warn once per session when a script observed-data source is sourced; moved rlang to a hard dependency.
- 2026-07-22 12:14: Warn (instead of dropping silently) when a programmatic observed-data source has no data behind it.
- 2026-07-22 12:18: On save, a programmatically added DataSet is now written to a PKML file so it survives a reload.
- 2026-07-22 12:22: Updated the observed-data vignette and NEWS for the round-trip and the script-vs-programmatic safety trade-off.
- 2026-07-22 12:35: Fixed a data-loss bug when saving a programmatic DataSet; the whole batch is validated before any PKML is written.
- 2026-07-22 13:10: Addressed PR review on the observed-data work: keep a session DataSet recoverable if a save aborts, guard name-less and unsafe-named sentinels, warn per project, and align the log format with the rule.
- 2026-07-22 14:30: Gave the parameter-set entry and parameter-identification `add*` functions a uniform overwrite policy: a duplicate now errors unless `overwrite = TRUE`.
- 2026-07-22 14:45: Extended the overwrite policy to `addOutputPath()`: an existing id errors unless `overwrite = TRUE`.
- 2026-07-22 15:10: Extended the overwrite policy to `addIndividual()`: an existing id errors unless `overwrite = TRUE`.
- 2026-07-22 15:20: Extended the overwrite policy to `addPopulation()`: an existing id errors unless `overwrite = TRUE`.
- 2026-07-22 15:30: Extended the overwrite policy to `addApplication()`: an existing id errors unless `overwrite = TRUE`.
- 2026-07-22 15:45: Extended the overwrite policy to `addParameterSet()` and `addInitialConditions()`: an existing set id errors unless `overwrite = TRUE`.
- 2026-07-22 16:00: Extended the overwrite policy to `addScenario()`: an existing id errors unless `overwrite = TRUE` (distinct from the `overwriteFormulasInSS` model option).
- 2026-07-22 16:20: Extended the overwrite policy to `addPlot()`, `addPlotGrid()`, and `addDataCombined()`: an existing id errors unless `overwrite = TRUE`.
- 2026-07-22 16:45: Extended the overwrite policy to `addObservedData()`: a source whose id (DataSet name or file basename) already exists errors unless `overwrite = TRUE`.
- 2026-07-22 18:30: Addressed PR review on the overwrite policy: shared `.assertNoOverwriteClash()` helper + catalog message across the `add*` functions, warn when overwriting a referenced parameter/initial-condition set, attribute entry-add aborts to the public function, validate `overwrite` passed through `...`, and make `addObservedData()` overwrite abort clearly on a file-based name collision without growing the name cache.
- 2026-07-22 15:00: Unified R6 method and helper casing to camelCase: validationResult methods, Project's parsing-era private methods, and the shared validation check helpers.
- 2026-07-22 15:30: Dropped the severity/kind prefixes from messages catalog entry names (error/stop/warning/validation/message), naming each by what it describes; kept a short context qualifier where two entries would otherwise collide.
- 2026-07-22 16:00: Normalized @family roxygen values to camelCase (spaced and kebab-case ones), leaving singular/plural as-is; updated the matching has_concept() entry in _pkgdown.yml.
- 2026-07-23 11:34: Removed three unused test helpers (testProjectExcelConfigurationsPath, executeWithTestFile, createValidPISheets) from tests/testthat/helpers.R. (#1059)
- 2026-07-23 11:35: Removed orphan test fixtures unreferenced by any test (ObsDataAciclovir_1/2/3.pkml, ProjectConfiguration-V5.xlsx). (#1059)
- 2026-07-23 11:40: Merged the duplicated settings test file into test-esqlabs-env.R, keeping the exact esqlabsRSettingNames list check and the snapshot error test; removed test-esqlabsr-settings.R. (#1059)
- 2026-07-23 11:45: Switched the two sensitivity xlsx-write tests to withr::local_tempfile() so nothing is written into the package test directory and an early failure cannot orphan the file. (#1059)
- 2026-07-23 11:50: Converted all bare set.seed() calls in the sensitivity and plot/population test files to withr::local_seed(), so a test no longer leaks the session RNG state past its own block. (#1059)
- 2026-07-23 11:59: Added `stopIfFails` to `runScenarios()`, defaulting to abort when a scenario produces no results (#1036).
- 2026-07-23 12:01: Extracted the shared scenario build loop into `.buildScenarioSimulations()`; `runScenarios()` now builds through it (prep for `buildSimulations()`, #1074).
- 2026-07-23 12:05: Added exported `buildSimulations()` returning parameterized-but-unrun simulations per scenario (#1074).
- 2026-07-23 12:08: Gave population entries an optional `type` (`programmatic`/`csv`): type-aware parse, validation, and a source-aware print (#1024).
- 2026-07-23 12:11: `addPopulation()` now accepts an `ospsuite::Population` object (session injection), with a runtime store, accessor, and reload reset (#1024).
- 2026-07-23 12:16: The run path resolves a population by its entry `type` (programmatic from the store, csv from its file, else demographics spec), scenario CSV flag kept for back-compat (#1024).
- 2026-07-23 12:19: `saveProject()` freezes an injected `Population` to `<id>.csv` and rewrites its entry to a csv source, so it survives reload (#1024).
- 2026-07-23 12:29: Addressed review: orphan programmatic population sentinel now serializes verbatim (no save-blocking abort); moved two run-path aborts to the messages catalog; shared the scenario build preflight (#1130).
- 2026-07-23 14:20: Dropped three no-op local_seed() calls before summarizer() in the PK-parameter tidy-dataframe test; summarizer() is pure dplyr and consumes no RNG. (#1059)
- 2026-07-23 14:31: ponytail cleanup: inline single-use `.asPopulation`, shrink `.populationEntryRequiredFields`, fold `.resolveProgrammaticPopulation` into its switch arm (#1130).
- 2026-07-23 14:45: Made the esqlabsRSettingNames test order-insensitive (set-equality on the names plus an enum-shape check) instead of asserting a fixed list order, since the names derive from an environment whose order is not guaranteed. (#1059)
- 2026-07-23 15:06: PR review: narrow injected-population dispatch to a real `Population` object; clean up inline abort strings (#1024).
- 2026-07-23 16:17: runScenarios()/buildSimulations() now canonicalize the `scenarios` argument before matching, so the name a scenario was authored with resolves to its canonical id (the same lowercasing/character-substitution ids get); a rewritten name warns. (#1157)
- 2026-07-23 16:19: Canonicalize commas and interior spaces out of definition ids (filename-safe), fixing literal-comma parameter-set filenames on Excel import. (#1158)
- 2026-07-23 16:22: addPITask()/PITask() can now create a task with no parameters or output mappings, so addPIParameter()/addPIOutputMapping() can seed the first one; a task left empty is flagged as a critical error by validateProject(). (#1157)
- 2026-07-23 16:33: PIOutputMapping()/addPITask()/addPIOutputMapping() now accept either an output-path id or the literal model path of a defined output path for `outputPath`, resolving both to the id; the two divergent not-found aborts are unified into one message-catalog entry with a did-you-mean hint. (#1157)
- 2026-07-23 16:37: NEWS entries for the three #1157 authoring-API ergonomics fixes. (#1157)
- 2026-07-23 16:39: Excel import builds one application per protocol sheet for the 5.x one-sheet-per-protocol layout, so scenario application refs resolve. (#1158)
- 2026-07-23 16:39: Excel multi-value cells parse the legacy quoted-CSV convention (strip quotes, keep a quoted comma in one token) alongside backslash escaping. (#1158)
- 2026-07-23 16:42: Excel import defaults `OverwriteFormulasInSS` to FALSE when the Scenarios sheet omits the column, instead of aborting; a pre-6.0 sheet now imports. (#1158)
- 2026-07-23 16:43: Excel import links an individual to its own override parameter-set sheet (matched by canonical id), so the override is applied instead of orphaned. (#1158)
- 2026-07-23 16:51: `addPopulation()` / `setPopulation()` accept `proteinOntogenies`, matching individuals and the Excel importer, so an imported population can be re-authored. (#1158)
- 2026-07-23 16:51: Excel import defaults a blank steady-state time/unit to `1000` / `"min"` (matching the authoring API), so a round-tripped project has no steady-state diff. (#1158)
- 2026-07-23 16:55: Excel import reifies the configured `dataFile` as an `excel` observed-data definition (with its sheets and importer config), warning if the file is missing. (#1158)
- 2026-07-23 17:03: Excel import parses the legacy 5.x parameter-identification layout (PITaskName-keyed sheets, no PITasks sheet), reassembling each task and resolving full-path output-mapping refs to output-path ids. (#1158)
- 2026-07-23 17:08: Reconcile NEWS.md dev section for the Excel legacy-migration work (canonicalization char list, individual-sheet auto-link, new importer bullet). (#1158)
- 2026-07-23 16:41: Fixed the second buildSimulations unknown-name test to expect the canonicalized name in the error. (#1157)
- 2026-07-23 17:57: PR review: dropped suppressWarnings on the runScenarios/buildSimulations canonicalization (a rewritten name now warns, surfacing a wrong-scenario match) and corrected the docs to say full canonicalization not just case; guarded .matchOutputPathRef against a non-scalar outputPath and hardened .nearestMatch so a NULL/multi-length value aborts with the typed message; aligned the addPITask outputPath-not-found error, addPITask roxygen, and NEWS wording. (#1157)
- 2026-07-23 18:01: PR #1160 review: guard blank/NA cells in the 5.x Excel parsers (blank IndividualId, blank Parameter Name, boolean OptionValue) and keep an observed-data subfolder path. (#1158)
- 2026-07-23 23:26: restoreProject() now detects a previous-version monolithic project snapshot (the single JSON from an older snapshotProjectConfiguration()) and upgrades it to the v6 tree by rebuilding the mirrored Excel workbooks and running them through importProjectFromExcel(); new R/import-legacy-snapshot.R plus a message-catalog entry and a fixture-driven test. (#1156)
- 2026-07-23 23:52: PR #1161 review: legacy-snapshot upgrade now reads row cells by column name (not position), guards a malformed sheet with a clear abort, and basenames a populations-CSV filename so it cannot escape the target folder. (#1156)
- 2026-07-24 00:20: PR #1161 review round 2: legacy-snapshot upgrade now muffles the redundant "data file not found" warning (was failing under testthat 3e), writes each workbook under the filename its projectConfiguration records (not a hardcoded name), keeps a zero-padded id column as text through type restoration, and emits the stale-handle warning on an overwriting restore like the v6 path. Classed the importer's skipped-observed-data warning so it can be muffled. (#1156)
- 2026-07-24 00:34: PR #1161 CodeRabbit: tighten .legacySheetToDf() sheet validation, a present-but-malformed `rows` (scalar or non-list record) now raises the clear malformed-sheet message instead of a cryptic indexing error; an absent `rows` stays a valid empty sheet. (#1156)
- 2026-07-24 07:30: PR #1160 review: guard `NULL` before subsetting in `.pi5xTaskRows()`, so importing a legacy 5.x workbook missing optional PI sheets returns an empty frame instead of aborting. (#1158)
- 2026-07-24 09:00: Gender is now optional on an individual, defaulting to UNKNOWN when absent (importer, addIndividual(), and the validator), so an animal species whose only valid PK-Sim gender is UNKNOWN can be represented and run. (#1162)
- 2026-07-24 09:20: runScenarios() no longer crashes on an individual that carries no age/height; the missing biometric is passed through as NULL (which ospsuite defaults) instead of being coerced to a length-zero numeric. (#1162)
- 2026-07-24 09:45: runScenarios(stopIfFails = FALSE) now also skips a scenario that fails at build time (was: only result collection was guarded, so a build failure aborted the whole batch); the broken scenario warns and collects as no-results while the rest run. buildSimulations() keeps its abort-on-failure contract. (#1170)
- 2026-07-24 10:30: review fixes for the build-time-skip work: result collection no longer calls getAllQuantitiesMatching() on a skipped scenario's NULL simulation; a skipped scenario's slot is kept as an explicit NULL (single-bracket assignment) instead of being deleted, which had shortened the list and broken the run loops when a non-last scenario failed; runScenarios() docs and NEWS updated for the build-time skip; shared runSimulations no-results test mock. (#1170)
- 2026-07-24 14:19: dataCombined validation now runs on the JSON spec directly via a `dataCombined` validation adapter (removed the legacy DataFrame reshape and the `FromExcel` validator); moved dataCombined shape validation out of the plots adapter so an error is no longer reported twice, and `createDataCombined()` / `createPlots()` pre-flight the `dataCombined` section so a bad definition aborts cleanly. (#1026)
- 2026-07-24 14:19: `addDataCombined()` simulated entries accept an output-path id for `path`, resolved to the literal path when the DataCombined is built; dropping a missing-dataSet observed entry now also skips its transform. (#1042)
- 2026-07-27 09:10: PR #1178 review: the gender-to-UNKNOWN default now also applies on the JSON-load path (`.parseIndividuals()`), so a hand-authored gender-less individual behaves like one from the authoring API or Excel; `validateProject()` flags a present-but-invalid gender token; a blank or whitespace-only Excel Gender cell counts as absent; and a scenario skipped at build time warns once instead of twice. (#1162, #1170)
- 2026-07-27 09:40: The deprecated `paramSheets` argument now actually warns: the deprecation is raised in `createScenariosFromPKML()` itself instead of three layers down, where lifecycle could not tell the call came from a user and so stayed silent. Fixes the long-failing soft-deprecation test.
- 2026-07-27 11:05: Excel import no longer loses a parameter set when two workbooks use the same sheet name. The workbook read first keeps the plain id, the later sheet is renamed, and the references that workbook makes (an individual's own sheet, an application's protocol sheet, their parameter-set columns) now follow the rename instead of quietly resolving to the other workbook's set. A warning names each rename. (#1163)
- 2026-07-27 11:20: An imported data combination no longer holds two spellings of one scenario: the observed block's scenario reference is now lowercased to the canonical id the same way the simulated block's already was. (#1165)
- 2026-07-27 11:45: Importing an Excel project into a different folder now brings the referenced models, data, and population folders along, so the result actually runs instead of ending in File-Not-Found warnings. The results folder, an out-of-project folder, and an in-place import are left alone; a folder the configuration names but the Excel project lacks is reported. (#1164)
- 2026-07-27 12:10: Importing an Excel project now says what it did (project written, definition counts per section, folders copied or missing) instead of finishing silently outside an interactive session. `silent = TRUE` turns it off. (#1166)
- 2026-07-27 13:05: Review fixes for the import work: a `../`-climbing folder value in the project configuration is no longer copied (it read outside the Excel project and wrote outside the output folder, overwriting whatever sat beside it), now contained the same way every other author-controlled path in the importer is; plus tests for the not-copied cases and two test comments reworded to describe the current behaviour rather than the old bug. (#1164)
- 2026-07-27 13:15: `addParameterEntry()` now takes a missing unit the way a spreadsheet gives it: an `NA` cell, or no `units` argument at all, means the value is in a base unit instead of aborting. (#1167)
- 2026-07-27 13:30: `initProject()` now creates the destination folder when it is missing instead of refusing to start, so the very first call of the authoring workflow no longer needs a `dir.create()` in front of it. (#1173)
- 2026-07-27 14:05: A scenario's simulation time can now be given the way it is usually held in R, as the numbers `c(start, end, resolution)`, instead of only as the "0, 42, 48" string; a list gives one grid per scenario. Clearing the field no longer makes a later read of it return the time unit. (#1168)
- 2026-07-27 14:45: An observed-data declaration can now carry an `id`, and that id names its file on disk and is what removes it, so observed data is filed by id like every other section instead of by the data file's name (which produced the odd doubled `.xlsx.json`). Declarations without an id behave exactly as before. (#1172)
- 2026-07-27 15:05: `addObservedData()` now spells out what its `entry` list must contain, per source type, and says that the file paths in it are relative to the project's data folder, so the shape no longer has to be reverse-engineered from the error messages. (#1171)
- 2026-07-27 15:30: PR #1179 review: a status check no longer copies the whole model/data tree (the internal Excel comparison imports with the new `copyAssets = FALSE`); `overwrite` now governs the asset folders too, so a file you placed in the output yourself is not replaced; the imported-in-place check resolves symlinks instead of comparing path spellings; a parameter-set reference that differs only in case now follows a rename; and the rename warning pluralizes correctly and no longer blames "an earlier workbook" when both sheets are in the same file. (#1163, #1164)
- 2026-07-27 15:55: `createScenariosFromPKML()` takes the numeric simulation-time grid too, so all three ways of setting a scenario's time now accept the same forms. (#1168)
- 2026-07-27 16:10: The observed-data article now tells readers they can name a source themselves with an `id`, next to where it explains the two handles a source carries. (#1172)
- 2026-07-27 17:20: PR #1180 review: an observed-data definition file whose id disagrees with its filename, or whose id is not a string, now fails the load with the file named, instead of being silently re-filed on the next save or crashing somewhere else entirely. Adding a source rejects an unusable id up front, spots a clash with a hand-chosen id from either direction, and refuses to overwrite a session `DataSet` with a configuration entry (which would have stranded the data). (#1172)
- 2026-07-27 17:30: PR #1180 review: documentation follow-ups. `addObservedData()` says that overwriting only replaces a source of the same kind, the definitions folder is described as the default layout rather than a fixed path, `createScenariosFromPKML()` no longer claims the time unit is used only when a time grid is supplied, and the observed-data article's removal section mentions the declared id. (#1171, #1172)
- 2026-07-28 09:40: A notes, scratch or fit-bounds sheet sitting beside the parameter sheets of a parameter workbook no longer stops an Excel import: the importer now tells a parameter sheet from any other by its columns and skips the rest with a warning, and a skipped sheet no longer leaves an application or an individual pointing at a parameter set that was never created. (#1181)
- 2026-07-28 10:05: The rule that a path naming an environment variable is allowed to point outside the project now lives in one place, instead of the same pattern being spelled out at each of the three checks that apply it.
- 2026-07-28 10:35: A project whose observed data sits on a synced drive outside the project folder now migrates: the Excel import says the data is out of reach and carries on, instead of refusing the whole project, and a data folder written as an environment variable is resolved rather than taken literally. (#1182)
- 2026-07-28 11:10: PR #1188 review: a parameter workbook sheet whose name contains curly braces no longer crashes the import that was meant to tolerate it, and a data file that sits outside the data folder is now told so, instead of being told it is outside the project when it usually is not. (#1181, #1182)
- 2026-07-28 11:25: PR #1188 review: a folder path written as `$PATH/...` no longer skips the check that keeps a project's folders inside the project. `$PATH` is the one variable the package deliberately never substitutes, so such a path stays literal and was escaping unchecked. (#1182)
- 2026-07-28 11:50: PR #1188 review: printing a project whose data folder sits outside the project (a synced drive) now shows that folder instead of failing, so a freshly imported project can be inspected. Reading the folder still refuses it, and a folder written as an unset environment variable prints as the variable. (#1182)
- 2026-07-28 12:10: PR #1188 review: a data folder written as an environment variable that stands for a path inside the project is now resolved against the project file and copied along with it, so the folder found while importing is the folder found when the project is later opened. A variable that is not set is reported instead of passed over silently. (#1182)
- 2026-07-28 12:35: PR #1188 review: when a sheet is skipped for not being a parameter sheet, an application or individual whose parameter-set column named it now loses that reference too, rather than keeping a pointer to something that was never created. The four column names that define a parameter sheet are written down once and read by both the Excel reader and the importer. (#1181)
- 2026-07-28 13:05: A blank cell, or a missing column, in a legacy parameter-identification sheet no longer stops a project from being imported or restored. The four places that tested a cell for emptiness in a way that broke on an absent one now share a single test that handles it. (#1190)
- 2026-07-28 13:35: Rows that are empty in every column are now ignored when a spreadsheet is read, so the trailing empty rows an Excel file picks up over years of editing are no longer taken for definitions with a missing name. A row that is empty in only some columns is still read as before. (#1191)
- 2026-07-28 15:10: Two small shared pieces for the Excel reader: one test for "this cell has to be a number", and one place that emits an import warning safely when the warning quotes text a modeller wrote. No change in behaviour.
- 2026-07-28 15:40: A parameter row whose value cell holds text instead of a number is now skipped, with a warning naming its sheet, row and cell, instead of stopping the whole import. A fit-bounds sheet copied from a real parameter sheet no longer blocks a project's migration. A blank value cell still means "no value", as before. (#1189)
- 2026-07-28 16:15: A parameter-identification sheet from before the output-path column existed can now be imported. Such a sheet named its outputs through the scenario, so each row is turned into one mapping per output path its scenarios list, keeping the rest of the row. A row whose scenarios list no output path is skipped with a warning instead of blocking the whole restore. (#1192)
- 2026-07-28 16:50: Importing a project now says which data combinations have an observed curve with no data set named, and that the project will not pass its own check until that cell is filled. The rows are kept as authored rather than dropped, so the missing piece can still be supplied. The three warnings about observed data that could not be imported say the same thing. (#1183)
- 2026-07-28 17:30: Review round on the import fixes: a skipped parameter row is now reported at the row Excel shows rather than a count that ignores blank rows; a sheet whose every row was skipped no longer becomes an empty parameter set; the parameter-identification derivation matches a scenario however it is spelled and skips a row that names no data set, which would otherwise have failed a moment later; and the note about a blank output-path cell now says what actually happens. (#1183, #1189, #1192)
- 2026-07-28 18:05: A project whose parameter-identification mapping names no output path or no observed data now opens, and the project check reports it, instead of the load stopping. A workbook with one such blank cell was impossible to open and so impossible to fix. Creating such a mapping by hand is still refused. (#1192)
- 2026-07-29 09:20: A project whose parameter-identification parameter has no path or is missing a bound now opens too, matching the output mappings, so a spreadsheet with one blank bounds cell can be opened and fixed. Every gap in a task is now listed in one report, because the missing-field checks moved to the section check, which is not skipped the way the cross-reference check is. (#1192)
- 2026-07-29 10:19: `isProjectInitialized()` now reports a project only when a `Project.json` is present, so a folder holding just a stray spreadsheet or an empty `Configurations` folder is no longer mistaken for a project. `initProject()` keeps its own broader check, so it still asks before scaffolding over an Excel project that has not been migrated. (#1194)
- 2026-07-29 11:05: Wrote down that a pull-request stack must be rooted at json-based-project, never at v6 or main. A stack that reaches lower makes the v5 to v6 migration part of it, so the top pull request is shown a conflict that belongs to the branches underneath it, and rebasing to clear it would rewrite branches every other pull request is based on.
- 2026-07-29 11:17: A project is now recognised by what its container JSON declares rather than by being called `Project.json`, so a project imported from an Excel workbook under another name is found too, and overwriting a project no longer leaves its old container behind. (#1194)
- 2026-07-29 11:50: Brought the sensitivity analysis correctness fixes over from the v6 branch, which had them and this branch did not. Percent change and sensitivity now take each output path's own baseline instead of relying on the rows happening to be in the right order, which could silently pair an output with another one's baseline when several outputs were analysed. Asking for absolute variation on a parameter whose starting value is zero now says so instead of quietly producing meaningless scaling factors. A parameter whose every run failed is reported and left out rather than stopping the whole analysis. Reloading a saved analysis no longer refuses valid results that used a different variation range per parameter, absolute variation, or that dropped failed runs. A tornado plot now accepts a reciprocal factor typed by hand, such as 0.3 with 3.333333, which exact matching rejected. (#1056)
- 2026-07-29 12:03: A saved project now records the esqlabsR version that wrote the file, instead of keeping the version stored in the file it was read from. The format version is written the same way, by whoever writes the file.
- 2026-07-29 12:05: A saved sensitivity analysis now carries its own copy of the simulation, so the folder can be moved, shared, or reloaded after the original model file has been renamed or deleted. Reloading prefers that bundled copy and falls back to the recorded path for folders saved earlier, so existing saves keep working. Saving also no longer fails when the first parameter's runs all failed, because it now looks for the first run that did produce results instead of assuming the first one did. (#878)
- 2026-07-29 12:25: The last test file that loaded a simulation while the file was merely being read, rather than inside a test, now loads it on first use like the sensitivity test files already did. Loading a simulation starts a PK-Sim session, and the test runner reads every test file up front, so that one line started a session that then leaked into unrelated files. This was the only thing standing between the suite and running its files in several processes at once, which the whole suite now passes. (#1005)
- 2026-07-29 12:50: The plot-results article is now a Quarto document and carries the diagram of how a data combination, a plot and a plot grid relate, which the v6 branch had and this branch did not. The diagram is redrawn for the JSON model: the v6 one still named the Excel sheets and columns it was drawn from. Quarto is needed because a Mermaid diagram does not render in a plain R Markdown vignette. The article keeps its plot-results name, which eleven cross-references in other articles rely on. The website build needed no change: it spots a Quarto file on its own and installs what it needs. The package check did need one, because Quarto renders the article in a separate R session that would otherwise use an already-installed copy of the package rather than the one being checked. (#1014)
- 2026-07-29 13:10: The plot helper tests had the same ten-colours check twice, word for word, so the test report listed one title twice and counted it twice. The second copy is gone.
- 2026-07-29 14:30: Merged the v6 branch into this one so the migration pull request can go in cleanly. Every conflict was settled in favour of this branch, which is safe now that the fixes v6 had and this branch lacked were brought over first. Four things the merge got silently wrong were put right by hand: the version number had been dragged back to a 5.7.0 development number, the showtext workaround had been pulled back into package load where it changes behaviour for every user rather than only for tests, the whole 6.0.0 changelog had been filed under the already-released 5.7.0 heading, and one sensitivity test ended up in the file twice. The seven separate workflow files from v6 replace the single one, and the nightly released-dependency check now installs Quarto for the plot-results article. (#1088)
- 2026-07-30 10:28: `addScenario()` and `setScenario()` now read a zero-length reference list (the `[]` a definition file carries) as "there are none" instead of aborting. (#1187)
- 2026-07-30 10:33: The reference error `addScenario()` and `setScenario()` raise for an id that does not exist now names the closest existing id, the same "did you mean" hint the project validator already gives. (#1169)
- 2026-07-30 10:35: Documented that authoring checks a reference the moment it is written while loading and importing leave it to the project validator, and that an empty reference list means "there are none". (#1169)
- 2026-07-30 11:12: A scenario reference list read the way the package reads its definition files (a list of id strings) is now accepted by `addScenario()` and `setScenario()`, so a scenario with references can also be rebuilt from its own written fields. (#1187)
- 2026-07-30 11:12: Narrowed the shared authoring documentation to what each section actually does (the empty-list rule and the "did you mean" hint are scenario-only), noted that `validate = FALSE` skips the pre-run check, and recorded the empty-list behaviour in the changelog. (#1187, #1169)
- 2026-07-30 12:05: A plot grid whose Excel `plotIDs` cell wraps each plot id in quotes (the 5.x way of allowing a comma inside a value) now imports with its members intact; the quotes used to become underscores, so every grid member looked like an unknown plot. (#1184)
- 2026-07-30 12:10: The project validator now matches a plot reference against its definition the same way ids are matched everywhere else, so a case-only difference resolves, and when a reference really has no match the error names the closest existing id. (#1184)
- 2026-07-30 14:20: An Excel import now writes its project file as `Project.json` whatever the source workbook was called, so `loadProject()` opens an imported project the same way it opens any other; the new `projectFileName` argument names that file something else. (#1174)
- 2026-07-30 14:25: Updated the comments that still described the imported project file as being named after the Excel workbook. (#1174)
- 2026-07-30 15:05: Took back the case-insensitive matching of plot references added earlier today. The project validator matches a plot reference exactly again, the same way the plot builder resolves it, so the validator can no longer call a project clean that `createPlots()` then refuses; an empty reference stays reportable, and the "did you mean" hint stays. (#1184)
- 2026-07-30 15:10: An Excel export now names its workbook after the project file, so the file `projectStatus()` looks for is the one exporting writes; the import appends `.json` rather than replacing an existing extension, so a dotted name like `trial.v1` stays its own project; and the two identical filename guards became one shared helper that blames the function the user called. (#1174)
- 2026-07-31 09:50: The OSP dependencies now come from the Open Systems Pharmacology R-universe instead of being reinstalled from GitHub: `DESCRIPTION` declares the universe and no longer lists `Remotes`, the CI workflows pass that repository to the checks, and the two nightly checks became one plain r-lib check. The install instructions add the universe to `repos` first, because `Additional_repositories` is metadata that `pak` does not read.
- 2026-07-31 14:30: Opening a project snapshot written by a previous esqlabsR version now says so and names `restoreProject()`, the call that upgrades and opens it, instead of reporting an unsupported schema version; the same message covers every entrypoint that opens a project file, and a project file's problems are no longer blamed on an internal method name. (#1185)
- 2026-07-31 14:52: Importing an Excel project whose individuals or populations sheet omits an optional column (`Protein Ontogenies` on an individual with no ontogenies, a biometric an older sheet never carried) no longer shows the reader a raw "Unknown or uninitialised column" warning; the two sheets are now read cell by cell the same way the scenarios sheet already was, which yields exactly the same values. (#1186)
- 2026-07-31 15:18: Settled which of the two message-building helpers the catalog uses, and wrote the reason down next to the code and in the refactoring brief: `cliFormat()` formats one inline string and silently drops the bullet names of a multi-part message, so it is the default for a single-line message while `cli::format_message()` stays for anything with bullets. Five single-bullet messages moved to `cliFormat()`, which also stops them printing a doubled marker. (#1122)
- 2026-07-31 16:05: Authoring over a project full of non-canonical ids now reports each rewritten id once per call instead of once per id: the collector that already grouped a call's reference rewrites is opened once for the whole authoring call and the definition's own id feeds it too, so one warning lists everything that call renamed. Canonicalizations done only to compare or re-key say nothing, as before, now through an explicit silent wrapper rather than `suppressWarnings()`, which no longer reaches a grouped warning. (#1175)
- 2026-07-31 17:40: Addressed the review of the messages work: the redaction in the new previous-version-snapshot test now also matches a Windows drive-prefixed path, so that check stops failing on Windows; the message-helper rule says accurately that a wrapped template's `\` continuation is resolved by the raising call rather than by `cliFormat()`, and names the third form the catalog uses (an unglued template, for a value that may itself contain braces), which the unsupported-schema-version message now follows; and an individuals or populations sheet missing a column the importer requires says so, naming the sheet and the columns, instead of going quiet and failing later on a value that names nothing. (#1149, #1185, #1186, #1122)
- 2026-07-31 17:45: Cleared the package check's warning and five of its notes: the two deprecated `*FromExcel()` shims document their `...`, `modifyList()` is called from its own package, the R6 class builder is imported so it counts as used, the data-mask column names are declared, a stray pair of braces left the validation-print docs, three sensitivity examples wrap under the manual's line limit, and `.Rbuildignore` now actually excludes the refactoring notes (its pattern matched `REFACTORIN`, not `REFACTORING.md`) along with the agent and git directories.
- 2026-08-03 10:15: Added a second Excel test project, `TestProjectExcelLegacy/`, shaped the way workbooks older than 5.6 actually are: ontogenies in a two-column `Protein` + `Ontogeny` pair, no `OverwriteFormulasInSS` column, quoted multi-value cells, a populations CSV folder under the configurations folder, and the 5.x parameter-identification sheets. The existing Excel fixture uses the modern spelling of every one of those, which is why it could never show what the importer does with the old ones. A generator script under `data-raw/` rebuilds the workbooks so they stay reviewable, and three test helpers copy the fixture, mutate one workbook of the copy, and import it while collecting whatever the import said. (#1213)
- 2026-08-03 11:40: Pinned what the importer currently does with the legacy fixture's pervasive traits, one test per finding, each passing against today's behaviour so it flips when the fix lands: every ontogeny declared in the old two-column form is dropped without a word and leaves no trace to check against; two parameter-identification rows that shared a `Group` (one free parameter in 5.x) become two independent ones; a legacy `Units` cell is carried through; a parameter-identification reference to an observed data set the project does not define is graded as no error at all; a blank simulation-time unit imports as nothing while authoring the same scenario gives it hours; a plots field typed as text in the workbook stays a string; a quoted single-value reference keeps its quotes as underscores in the id; and the populations CSV folder never travels with the imported project, with the one report that would have said so switched off. (#1213)
- 2026-08-03 12:35: Pinned five more findings, each from a variant of the legacy fixture that changes exactly one workbook: a configuration workbook the property sheet names but that is not on disk imports as an empty section with nothing said about it, and the summary leaves the zero out; a parameter sheet with repeated column headers is reported as skipped, and that one report is all the user gets while the individual named after it quietly loses its parametrization; an individual with a parameter sheet but no biometrics row disappears, leaving an ownerless parameter set and a scenario pointing at nothing; two plot rows sharing an id lose one plot to the other with no duplicate reported; and an id containing a no-break or zero-width space keeps it through canonicalization and into the definition filename, so two ids that look identical become two files and the project still validates clean. (#1213)
- 2026-08-03 13:20: Pinned the three findings that live outside the importer, at the functions that own them: reading more than one protein ontogeny aborts (unreachable today only because the importer drops them all, so repairing that is what exposes this); id canonicalization leaves a no-break space, a zero-width space and the other invisible formatting characters untouched, while turning an ordinary space into an underscore; the "did you mean" hint cannot reach a candidate that differs by a real suffix such as `_mean`, which is why the previous migration run could not classify the reference it was pointed at; and the population csv filename the runner derives is lowercased, so it cannot match the mixed-case file the project actually carries. (#1213)
- 2026-08-03 14:05: Strengthened the ontogeny pin: instead of noting that two populations both come out with no ontogenies, the test now adds a twin of one population differing in nothing but its ontogenies and shows the two definition files come out identical apart from the id, which is the sharpest statement of the loss and the one the migration testing actually made. (#1213)
- 2026-08-03 15:10: Tightened the invisible-character test to assert the import raised no warning at all, rather than that no warning contained the letters "id", which would have failed on any unrelated message that happened to contain them. (#1213)
- 2026-08-03 15:21: Running two scenarios that share a population id but read it differently (one from its demographics spec, one from its csv file) in one batch no longer hands the second scenario the first one's population: the run's population cache is now keyed on how the population is produced, not on its id alone, so each scenario gets what it asked for whatever order the batch runs in, and a warning names an id that stands for two different populations. (#1213)
- 2026-08-03 15:52: A scenario for a non-human individual runs again without needing `stopIfParameterNotFound = FALSE`. The bundled species-parameter sheet was being folded into the user's own parameters and then applied strictly, so any one of its ~245 paths that a given model happens not to have aborted the whole scenario; it is no longer a merge layer, leaving the single tolerant application that `initializeSimulation()` already did. A parameter path the user wrote themselves that the model lacks still aborts, as before. (#1213)
- 2026-08-03 15:55: A scenario that reads its population from a csv file now finds the file under the case its name is spelled with on disk, so a project whose csv is `TestPopulation.csv` no longer works on macOS and fails on Linux; a file that is not there is reported naming the file and the folder it was expected in, instead of failing inside the simulation backend. (#1213)
- 2026-08-03 16:00: `validateProject()` now reports a scenario whose population csv file is not there, graded like a missing model file. Such a project used to report no critical errors and no warning about it, and then fail once the scenario was run. (#1213)
- 2026-08-03 16:05: An Excel import now brings the population csv folder along and points the imported project at it. An Excel project names that folder relative to its configurations folder while a project resolves working folders against its own root, so the folder was neither copied nor resolvable, and a csv-population scenario failed at run time with the folder left behind in the Excel project. (#1213, #1164)
- 2026-08-03 16:10: Upgrading a project snapshot written by a previous esqlabsR version now carries the model, data and population folders sitting beside the snapshot file into the upgraded project, and names any that were not there. Nothing travelled at all before, and the report that would have said so was suppressed on exactly this path, so every scenario failed as File Not Found with no explanation. A working folder that would resolve outside the project is now refused before anything is written, instead of writing the whole project and then failing to open it. (#1213, #1164)
- 2026-08-03 16:15: Wrote the asset-travel and population-csv fixes into NEWS, editing the two bullets from this cycle that described the old behaviour rather than adding siblings beside them. (#1213)
- 2026-08-03 16:20: Settled the shape of a definition's protein ontogenies for hand-authoring: one `"Protein:Ontogeny"` entry per ontogeny (or the same pairs as one comma-joined string, as the Excel sheets spell it), documented on the four add/set functions and applied whole to a definition instead of being split across the ids, so two ontogenies on one individual or population can be written at all. A value that cannot be stored, an `ospsuite::MoleculeOntogeny` object above all, is refused at the call with a message about ontogenies rather than ids; it used to be accepted and then broke saving the whole project. (#1213)
- 2026-08-03 16:24: Running a scenario whose individual or population carries more than one protein ontogeny no longer aborts before reading any of them: the ontogenies are flattened first, from whichever shape they were stored in (one entry per ontogeny, one comma-joined cell, or the list a JSON array parses to), instead of the whole field being tested as if it were a single value. (#1213)
- 2026-08-03 16:28: Importing a legacy Excel project now brings in its protein ontogenies, which were discarded in silence: a 5.x workbook declares them as a `Protein` + `Ontogeny` column pair, and only the newer single-cell spelling was read, so two populations differing only in their ontogenies imported identically apart from the id. Both spellings are read, the pair is folded into the single-cell one so an export and re-import returns the same pairs, and a declaration whose two columns cannot be paired is reported naming the individual or population. (#1213, #1186)
- 2026-08-03 16:32: Wrote up the ontogeny work for users: what the legacy import now brings in, that more than one ontogeny runs, and the shape the four add/set functions accept. (#1213)
- 2026-08-03 17:28: Turned the legacy-fixture tests that recorded the old behaviour into tests of the fixed behaviour, now that the ontogeny import, the population csv folder and the csv filename case are all repaired: the fixture's ontogenies are asserted to arrive, two populations differing only in them to import differently, the population csv folder to travel and resolve where the csv file was copied, and the derived filename to find the mixed-case file on disk. (#1213)
- 2026-08-03 18:05: `loadProject()` now opens a project folder as readily as a project file, and defaults to the working directory, so a project named something other than `Project.json` (what the Excel import's `projectFileName` produces) opens with a bare call, and handing it the project folder no longer fails as unreadable JSON. A folder with no project, or with several, says which it is. (#1213)
- 2026-08-03 18:05: Definition ids are now canonicalized across Unicode, not just ASCII: compatibility spellings of one character are folded together (the micro sign and the Greek mu, a full-width letter and its ASCII form), a no-break space becomes the same underscore an ordinary space becomes, and an invisible formatting character (zero-width space, word joiner, byte-order mark, soft hyphen) is dropped instead of being carried into the definition filename. Two ids that render identically are therefore one id, reported as a collision rather than written as two look-alike files. The "did you mean" hint also had its cap of three characters removed, so the divergences that actually occur (a `_mean` sibling, a per-analyte suffix) are suggested. (#1213)
- 2026-08-03 18:35: A parameter-identification task can now be created with nothing in it: `addPITask(project, id)` needs only the id, and the new `setPITask()` gives the task its scenarios and solver configuration afterwards, which was the missing half of the create-then-seed shape. A task's `scenarios` follows the same empty-means-none rule as every other reference list instead of contradicting it, and a task left with no scenarios is reported alongside the missing parameters and mappings in one report. An application's `parameterSets` accepts the same empty list, which the rule had not reached. (#1213, #1206)
- 2026-08-03 18:45: The cross-reference check now always runs, and each operation is gated only on the references its own sections make. It used to stand itself down as soon as any section validator found a critical error, which is the very check `runScenarios()`, `createPlots()` and `runPI()` depend on, so a broken plot could remove the check that stops a simulation and a scenario naming a missing individual ran anyway; and conversely a plotting-only dangling reference blocked every run. A scenario pointing at a missing individual now stops the run, a data combination pointing at a missing scenario stops plotting and lets the run through, and `validateProject()` still reports every section. (#1213)
- 2026-08-03 19:05: A scenario read out of a project can now be handed straight back to `addScenario()` / `setScenario()`, which is how you copy one into another project or revise one by editing its own field. The record travels through the same translation the project's save writes with, so nothing was renamed and the written files are untouched: the resolved output paths become ids again, a multi-interval time grid becomes its one string, and the steady-state time returns to its declared unit. A list of intervals is also accepted directly, instead of being reported as the wrong number of ids. (#1213)
- 2026-08-03 19:05: Validation now reports a parameter-identification output mapping whose `observedData` names a data set none of the project's observed-data sources holds, with a hint naming the ones that do. It was the one reference the cross-reference check never resolved, so a project with several of them validated clean and `runPI()` was the first thing to notice, aborting while building the task. Because a data set's name lives inside its source, the check reads the sources to answer; a project holding a `script` source, or one whose sources cannot be read, leaves the reference alone rather than guessing. (#1213)
- 2026-08-03 19:10: A blank simulation-time-unit cell in a legacy Scenarios sheet now imports as the same hour `addScenario()` defaults to, instead of no unit at all, so the same blank cell no longer means two different units depending on whether the project was imported or hand-authored. (#1213)
- 2026-08-03 19:25: An imported project is no longer reported as out of sync with the Excel workbook it was just exported to. A plot field holding a number came back from the workbook as text, a record's fields came back in the sheet's column order rather than its own, and a parameter-identification task's nested configuration groups were flattened onto columns and never reassembled; a number now stays a number both ways, a nested configuration group survives the round trip, and field order is no longer read as a difference. (#1213)
- 2026-08-03 19:35: The observed-data declaration an Excel import reifies from the configured data file now states its own id, the canonicalized basename with the extension dropped, so its definition file is `my_data__v2.json` rather than `My Data, v2.xlsx.json` and the declaration is addressable by an id the authoring API could also have written. The file path it stores is untouched. Also corrected the legacy snapshot fixture, whose second parameter-identification mapping named a dose group its data file does not hold, which the new observed-data reference check correctly flagged. (#1213, #1172)
- 2026-08-03 19:45: A run that skips failing scenarios now says which ones: it closes by naming every scenario that produced no results, and the help documents `results = NULL` as the marker plus the one-liner for keeping the scenarios that ran. A build failure names the scenario whatever caused it, so a message from the simulation backend that names neither the scenario nor the file is no longer all the user gets, and a `.pkml` that is still a Git LFS pointer is named as one instead of failing as broken XML. Aborts from a run are attributed to `runScenarios()` / `buildSimulations()` rather than to an internal helper. (#1213)
- 2026-08-03 19:45: Importing a legacy parameter-identification workbook now reads its `Group` column, so rows that describe one free parameter fitted across several scenarios stay one parameter instead of becoming several independent ones, each with its own estimate. A group whose rows disagree about the bounds cannot be one parameter, so those rows are left as they are and the group is named, rather than the whole migration stopping the way 5.x did. (#1213)
- 2026-08-03 20:05: A parameter identification no longer refuses to run because a parameter declares a unit belonging to another quantity. Legacy workbooks carry a units column that esqlabsR 5.x read past, so such a cell is normal in a project that used to run; the unit is now reported and left unapplied, with the bounds read in the parameter's own unit, instead of stopping the task. (#1213)
- 2026-08-03 20:10: Straightened out what the docs and messages say about the project's own fields. The authoring help no longer promises an edit is "persisted in a single write-through", which contradicts the explicit-save model: an edit is in memory until `saveProject()`, and every add/set/remove topic now says so. The paths block prints with a header saying the folders are shown relative to the project folder, and the field help says reading one gives the absolute path already, so it must not be joined onto the project folder again; the same help names assigning `project$info` / `project$paths` as the way to change a project's own fields, which had looked like a missing API. A rejected project file name that is empty, `NA` or not text is described as such instead of being accused of containing a path separator, and restoring a project warns about a dangling reference once rather than twice. (#1213)
- 2026-08-03 20:20: Importing a plots workbook now names a plot id (or grid name) that more than one row uses. Every such row after the first was quietly folded into one definition and the earlier row's plot disappeared, while the reported plot count only counted the survivors. (#1213)
- 2026-08-03 20:40: Three authoring ergonomics from the migration run. `addObservedData()` names every required field a declaration is missing in one go, so an Excel source no longer takes three attempts to complete. An `NA` in a numeric field of an individual or population now means "not set", the same as leaving it out, which is what an empty workbook cell reads as and what the Excel import already does with it; a value that is not `NA` but is not a number still aborts. And `setScenario(overwrite = TRUE)`, written out of habit from `addScenario()`, is refused instead of partial-matching onto `overwriteFormulasInSS` and quietly changing a steady-state model option. (#1213)
- 2026-08-03 20:45: An Excel import now says which individual loses its parametrization when its parameter sheet cannot be read, and which sheet describes an individual that has no biometrics row (so the individual is not imported at all and the scenario naming it dangles). Both losses used to be visible only as a sheet-level note, or not at all. (#1213)
- 2026-08-03 21:15: An Excel import now says which configuration workbooks it could not find, which ones it found but never read, which it read under a conventional name the project does not mention, and which sections ended up with nothing in them. All four were silent, so a project whose populations workbook was simply missing, or which kept a whole second set of workbooks the property sheet did not name, imported as if complete. (#1213)
- 2026-08-03 21:30: A parameter-identification task with no solver settings now writes an empty configuration object rather than an empty list, so its file describes the field the same way a task that has settings does. (#1213)
- 2026-08-03 21:50: An import now says when a legacy plots workbook carries figure-export settings (per-grid filename, width, height) that a project has no place for, instead of dropping the whole figure layout without a word. An empty sheet, which most workbooks carry, stays quiet. (#1213)
- 2026-08-03 22:05: A newly scaffolded project now records the esqlabsR version that actually created it, instead of the fixed version written into the template. Saving the project rewrote the field to the running version, so the number appeared to go backwards on the first save. (#1213)
- 2026-08-03 22:25: A project file now leaves out the name, description and default run options when they are not set, instead of writing them as null. An upgraded project used to look like one whose name had been deliberately emptied. (#1213)
- 2026-08-03 22:45: `restoreProject()` can now be told the name to give the project it writes, on both the current and the upgraded-snapshot path. A snapshot from a previous version carries no project name, so a restored project had none and no way to get one. (#1213)
- 2026-08-03 23:00: An import that has to invent an id now counts the same way everywhere: the second thing called `x` becomes `x_2`, whether it is a renamed parameter-set sheet (which used to become `x_1`) or a parameter-identification parameter or mapping. (#1213)
- 2026-08-03 23:20: An import now says when a legacy parameter-identification workbook points at an observed-data sheet that is not in the project's data file, so a mapping whose data set is simply not there is named at import instead of surfacing later as a dangling reference. A sheet that is in the file needs no mention, since every sheet of it is imported. (#1213)
- 2026-08-04 11:10: Every vignette is renamed and retitled so its name says what kind of article it is: one get-started tutorial, ten "how to" guides, two "about" explanations and two reference articles.
- 2026-08-04 11:25: New explanation article on the project model, gathering the reasoning that was scattered through the setup, scenario, validation and migration guides: why saving is explicit, why ids are rewritten, why a reference is checked when you author it but only reported when a project is read, and why Excel is now an interchange format.
- 2026-08-04 11:40: New explanation article on the plotting model, covering why a figure takes three definitions rather than one, and why observed data is stored as a reference to its source rather than copied into the project.
- 2026-08-04 11:55: New reference article describing the project folder, every field of the project file, and the record a simulation run returns. The folder listing now mentions the initial-conditions folder, which the old diagram left out.
- 2026-08-04 12:10: New reference article listing every field of every definition, whether it is required, and what happens when you leave it out, which is the question the get-started feedback issue asked. Built from the field lists in the code rather than from the old prose, which turned up two wrong statements. (#734)
- 2026-08-04 12:40: Each how-to guide is trimmed to the steps it actually describes, with the reasoning and the field tables replaced by a sentence and a link. Two corrections fell out: an individual needs only a species, not a species and a gender, and the README described saving as automatic when it is not.
- 2026-08-04 12:55: The plotting guide gains a worked population example showing how to choose the percentile bands, and points at the ospsuite.plots package for figures it cannot draw. (#771)
- 2026-08-04 13:05: The get-started article is now a single uninterrupted lesson: the file-layout digression and the mid-lesson links out are gone, and it closes by saying what each other article is for.
