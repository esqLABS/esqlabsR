# Possible entries for the `lloqMode` argument of `calculateMeans()`

Possible entries for the `lloqMode` argument of `calculateMeans()`

## Usage

``` r
LLOQMode
```

## Format

An object of class `list` of length 4.
