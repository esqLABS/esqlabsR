% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation.R
\name{validateProject}
\alias{validateProject}
\title{Validate a Project}
\usage{
validateProject(project)
}
\arguments{
\item{project}{A \code{Project} object (typically produced by
\code{\link[=loadProject]{loadProject()}}). Path inputs are not accepted here; load the
project first.}
}
\value{
Named list of \code{validationResult} objects with class
\code{"ValidationResults"}. Order matches \code{.validationAdapters},
with \code{crossReferences} last.
}
\description{
Runs every section validator (and a cross-reference pass) against a
parsed \code{Project} and returns a named list of \code{validationResult}
objects, one per section, in canonical order. On a clean run (no
section produced critical errors) it marks the project validated, so
subsequent \code{runScenarios()} / \code{createPlots()} calls can skip a
redundant validation pass until the next edit.
}
\examples{
\dontrun{
project <- loadProject("Project.json")
results <- validateProject(project)
if (isAnyCriticalErrors(results)) {
  print(validationSummary(results))
}
}
}
\seealso{
\code{\link[=isAnyCriticalErrors]{isAnyCriticalErrors()}}, \code{\link[=validationSummary]{validationSummary()}},
\code{\link[=print.ValidationResults]{print.ValidationResults()}}.
}
