% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation.R
\name{.ensureValid}
\alias{.ensureValid}
\title{Ensure a project passes validation before an operation}
\usage{
.ensureValid(project, sections, opName)
}
\arguments{
\item{project}{A \code{Project} object.}

\item{sections}{Non-empty character vector of section names required by
the calling operation.}

\item{opName}{Short label used in the abort message (e.g. \code{"runScenarios"}).}
}
\value{
\code{invisible(NULL)} on success.
}
\description{
Runs targeted validation for the sections an operation depends on, and
aborts with a formatted multi-error message if any critical errors are
found. Short-circuits when the project has been fully validated since
its last mutation (the \code{validatedSinceMutation} flag).
}
\details{
This helper does not itself flip the cache flag, because it only runs
a subset of validators. Only \code{\link[=validateProject]{validateProject()}} (a full run) sets the
flag.
}
\keyword{internal}
