% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utilities-simulation.R
\name{initializeSimulation}
\alias{initializeSimulation}
\title{Load a simulation and apply a set of parameters.}
\usage{
initializeSimulation(
  simulation,
  individualCharacteristics = NULL,
  additionalParams = NULL,
  simulateSteadyState = FALSE,
  steadyStateTime = 1000,
  ignoreIfFormula = TRUE,
  stopIfParameterNotFound = TRUE
)
}
\arguments{
\item{simulation}{\code{Simulation} loaded from a PKML file}

\item{individualCharacteristics}{Optional \code{IndividualCharacteristics}
describing an individual.}

\item{additionalParams}{Optional named list with lists 'paths', 'values', and
'units'.}

\item{simulateSteadyState}{Logical. If \code{TRUE}, the model is simulated for
\code{steadyStateTime} minutes after applying parameter values defined in
\code{individualCharacteristics} and \code{additionalParams}, and the end results
of the simulation are applied as initial conditions for all molecules.
Default is \code{FALSE}.}

\item{steadyStateTime}{Simulation time (minutes) for the steady-state
simulation. Must be long enough for system to reach a steady-state 1000 by
default.}

\item{ignoreIfFormula}{If \code{TRUE} (default), species and parameters with
initial values defined by a formula are not included in the steady-state
simulation}

\item{stopIfParameterNotFound}{Logical. If \code{TRUE} (default), an error is
thrown if any of the \code{additionalParams} does not exist. If \code{FALSE},
non-existent parameters are  ignored.}
}
\description{
Helper method that combines a set of common steps performed
before running a simulation. This method applies individual parameters data
set and additional user-defined parameters to the simulation and runs the
simulation to its steady-state and applies the steady-state as new initial
conditions.
}
\examples{
\dontrun{
simulation <- loadSimulation(filePath = modelPath)
humanIndividualCharacteristics <- createIndividualCharacteristics(
  species = Species$Human, population = HumanPopulation$European_ICRP_2002,
  gender = Gender$Male, weight = 70
)
userParams <- readParametersFromXLS(parameterXLSPath)
initializeSimulation(simulation, humanIndividualCharacteristics, userParams)
simulationResults <- runSimulation(simulation = simulation)
}
}
