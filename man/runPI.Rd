% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parameter-identification.R
\name{runPI}
\alias{runPI}
\title{Run Parameter Identification tasks defined in a Project}
\usage{
runPI(
  project,
  tasks = NULL,
  observedData = NULL,
  stopIfParameterNotFound = TRUE,
  simulationRunOptions = NULL
)
}
\arguments{
\item{project}{A \code{Project} object (see \code{\link[=loadProject]{loadProject()}}).}

\item{tasks}{Optional character vector of task ids to run. When
\code{NULL} (default), every task on the Project is run. The ids are
canonicalized the same way \code{addPITask()} canonicalizes a task id, so a
name typed as it was first passed to \code{addPITask()} still resolves.}

\item{observedData}{Optional named list of pre-loaded \code{DataSet}
objects that overrides automatic resolution from
\code{observedData} definitions.}

\item{stopIfParameterNotFound}{Logical. When \code{TRUE} (default), a
parameter listed in a scenario's parameter sets but absent from the
simulation aborts the build; when \code{FALSE}, it is skipped with a
warning. Forwarded through \code{.prepareScenario()} to
\code{initializeSimulation()}.}

\item{simulationRunOptions}{How the identification is executed: an
\href{https://www.open-systems-pharmacology.org/OSPSuite-R/reference/SimulationRunOptions.html}{SimulationRunOptions}
object setting \code{numberOfCores} and \code{showProgress}, for example
\code{ospsuite::SimulationRunOptions$new(numberOfCores = 8)}. \code{NULL} (default)
uses the \code{ospsuite} defaults. This is not project data, so a task's
\code{configuration} never supplies it; how the solver behaves does live there,
as the task's \code{solverSettings}.}
}
\value{
Named list of per-task results. Each entry is a list with
\code{task} (the runtime \code{ParameterIdentification} object), \code{result}
(the \code{PIResult} from \code{task$run()}, or \code{NULL} on optimisation
failure), and optional \code{error} (the optimiser's failure message,
absent on success).
}
\description{
Builds and runs every requested PI task in the Project. Build errors
(typos in parameter paths, unknown outputs, missing observed data, etc.)
propagate as hard errors so users can fix them immediately. Only the
optimisation step is wrapped in \code{tryCatch}: a numerical failure inside
\code{task$run()} degrades to \verb{result = NULL, error = <message>} so the loop
continues with the remaining tasks.
}
