% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parameter-identification.R
\name{runPI}
\alias{runPI}
\title{Run Parameter Identification tasks defined in a Project}
\usage{
runPI(
  project,
  piTaskNames = NULL,
  observedData = NULL,
  stopIfParameterNotFound = TRUE
)
}
\arguments{
\item{project}{A \code{Project} object (see \code{\link[=loadProject]{loadProject()}}).}

\item{piTaskNames}{Optional character vector. When \code{NULL} (default),
every task on the Project is run.}

\item{observedData}{Optional named list of pre-loaded \code{DataSet}
objects that overrides automatic resolution from
\code{project$observedData}.}

\item{stopIfParameterNotFound}{Logical. Forwarded to
\code{.prepareScenario()} for parameter merging.}
}
\value{
Named list of per-task results. Each entry is a list with
\code{task} (the runtime \code{ParameterIdentification} object), \code{result}
(the \code{PIResult} from \code{task$run()}, or \code{NULL} on failure), and
optional \code{error} (the failure message, absent on success).
}
\description{
Builds and runs every requested PI task in the Project. Each task is
built and executed inside a \code{tryCatch}; a build failure or
optimisation failure on one task degrades to \verb{result = NULL, error = <message>} and the loop continues.
}
