% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utilities-sensitivity-calculation.R
\name{saveSensitivityCalculation}
\alias{saveSensitivityCalculation}
\title{Save Sensitivity Calculation Results}
\usage{
saveSensitivityCalculation(
  sensitivityCalculation,
  outputDir,
  overwrite = FALSE
)
}
\arguments{
\item{sensitivityCalculation}{A named list of class \code{SensitivityCalculation}
as returned by \code{\link[=sensitivityCalculation]{sensitivityCalculation()}}, containing \code{simulationResults},
\code{outputPaths}, \code{parameterPaths}, and \code{pkData}.}

\item{outputDir}{A character string specifying the path to the directory
where the results should be saved.}

\item{overwrite}{Logical. If \code{TRUE}, an existing directory at \code{outputDir}
will be deleted and replaced. Default is \code{FALSE}.}
}
\value{
Invisibly returns \code{NULL}. Results are saved to disk in the specified folder.
}
\description{
Saves the results of a sensitivity analysis to a specified directory,
including metadata and simulation output required for restoring or sharing
the analysis.
}
\examples{
\dontrun{
sensitivityCalculation <- sensitivityCalculation(
  simulation = mySim,
  outputPaths = "Organism|PeripheralVenousBlood|Drug|Plasma",
  parameterPaths = c("Drug|Lipophilicity", "Application|Dose")
)

saveSensitivityCalculation(
  sensitivityCalculation,
  outputDir = "output/my-sensitivity",
  overwrite = TRUE
)
}
}
