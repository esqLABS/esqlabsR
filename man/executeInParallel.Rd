% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utilities-parallel.R
\name{executeInParallel}
\alias{executeInParallel}
\title{Parallelize the execution of a function over a list of arguments values}
\usage{
executeInParallel(
  fun,
  firstArguments,
  exports = NULL,
  ...,
  outputNames = NULL,
  nrOfCores = ospsuite::getOSPSuiteSetting("numberOfCores")
)
}
\arguments{
\item{fun}{A function that will be called with different arguments values}

\item{firstArguments}{A list of the values of the first argument of the
function. The function will be called \code{n} times where \code{n} is the number of
entries in \code{firstArguments}}

\item{exports}{Names of the objects in the calling environment that the
function relies on that are not passed as arguments. May be \code{NULL} (default).}

\item{...}{Further arguments of the function.}

\item{outputNames}{Optional: a list of names used for the output list. Result
of each execution of \code{fun} will be named with the name having the same
index in \code{outputNames} as as the argument value in \code{firstArguments}. If
specified, \code{outputNames} must have the same length as \code{firstArguments}}

\item{nrOfCores}{Optional: the maximal number of parallel threads. By default
the value defined in \code{ospsuite::getOSPSuiteSetting("numberOfCores")} is
used, and equals the number of logical cores minus 1.}
}
\value{
A list containing the outputs of the function \code{fun} iterated over the
values in \code{firstArguments}.
}
\description{
Parallelize the execution of a function over a list of arguments values
}
\seealso{
{\code{\link[=parLapply]{parLapply()}}}
}
