% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parameter-identification.R
\name{PIOutputMapping}
\alias{PIOutputMapping}
\title{Create a Parameter Identification output mapping}
\usage{
PIOutputMapping(
  id,
  scenarios,
  outputPath,
  observedData,
  scaling = NULL,
  xOffset = 0,
  yOffset = 0,
  xFactor = 1,
  yFactor = 1,
  weight = NULL
)
}
\arguments{
\item{id}{Character scalar. Identifier for this mapping, unique within
its task.}

\item{scenarios}{Character vector of scenario ids the mapping applies to.
Must be a subset of the task's own \code{scenarios}.}

\item{outputPath}{Character scalar identifying an output path defined in
\code{outputPaths}: either its id (a key of \code{outputPaths}) or the literal
model path it maps to (its value). Both resolve to the same defined
output path; it must already exist (add one with \code{\link[=addOutputPath]{addOutputPath()}}).}

\item{observedData}{Character scalar. Id of the observed dataset to fit
against.}

\item{scaling}{Optional character scalar. Residual scaling (e.g. \code{"lin"}
or \code{"log"}); \code{NULL} uses the runtime default.}

\item{xOffset, yOffset, xFactor, yFactor}{Numeric scalars. Affine transform
applied to the observed data before comparison. Defaults are the
identity transform (\code{0} offsets, \code{1} factors).}

\item{weight}{Optional numeric scalar or vector. Residual weight(s)
applied to the observed dataset; \code{NULL} leaves the data unweighted.}
}
\value{
A \code{PIOutputMapping} object: a named list with copy semantics.
}
\description{
Builds a plain-data \code{PIOutputMapping} record pairing one
simulation output with the observed dataset it is fitted against, plus
the optional per-mapping fitting metadata (scaling, axis offsets and
factors, residual weights).

A \code{PIOutputMapping} is a building block for a \code{\link[=PITask]{PITask()}}; pass a list
of them as the \code{outputMappings} argument of \code{\link[=PITask]{PITask()}} or \code{\link[=addPITask]{addPITask()}},
or add one to an existing task with \code{\link[=addPIOutputMapping]{addPIOutputMapping()}}.
}
\examples{
PIOutputMapping(
  id = "pvb",
  scenarios = "aciclovir_iv",
  outputPath = "aciclovir_pvb",
  observedData = "Laskin 1982.Group A",
  scaling = "log"
)
}
\seealso{
\code{\link[=PITask]{PITask()}}, \code{\link[=PIParameter]{PIParameter()}}, \code{\link[=addPIOutputMapping]{addPIOutputMapping()}}.

Other parameterIdentification:
\code{\link[=PIParameter]{PIParameter()}},
\code{\link[=PITask]{PITask()}},
\code{\link[=addPIOutputMapping]{addPIOutputMapping()}},
\code{\link[=addPIParameter]{addPIParameter()}},
\code{\link[=addPITask]{addPITask()}},
\code{\link[=removePIOutputMapping]{removePIOutputMapping()}},
\code{\link[=removePIParameter]{removePIParameter()}},
\code{\link[=removePITask]{removePITask()}}
}
\concept{parameterIdentification}
