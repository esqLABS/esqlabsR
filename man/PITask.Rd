% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parameter-identification.R
\name{PITask}
\alias{PITask}
\title{Create a Parameter Identification task}
\usage{
PITask(id, scenarios, parameters, outputMappings, configuration = list())
}
\arguments{
\item{id}{Character scalar. Identifier for the task.}

\item{scenarios}{Character vector of scenario ids the task runs against.
Every scenario referenced by a parameter or output mapping must be in
this set.}

\item{parameters}{List of \code{\link[=PIParameter]{PIParameter()}} records. May be empty to create
a task that is seeded later with \code{\link[=addPIParameter]{addPIParameter()}}; a task must have at
least one parameter to run, which \code{\link[=validateProject]{validateProject()}} enforces.}

\item{outputMappings}{List of \code{\link[=PIOutputMapping]{PIOutputMapping()}} records. May be empty to
create a task that is seeded later with \code{\link[=addPIOutputMapping]{addPIOutputMapping()}}; a task
must have at least one output mapping to run, which \code{\link[=validateProject]{validateProject()}}
enforces.}

\item{configuration}{Named list of solver settings (e.g. \code{algorithm},
\code{ciMethod}, \code{objectiveFunction}, \code{simulationRunOptions}). Defaults to
an empty list, leaving every runtime default in place.}
}
\value{
A \code{PITask} object: a named list with copy semantics.
}
\description{
Builds a plain-data \code{PITask} record bundling the scenarios,
the optimisation variables (\code{\link[=PIParameter]{PIParameter()}} records), the output
mappings (\code{\link[=PIOutputMapping]{PIOutputMapping()}} records), and the solver configuration of
one Parameter Identification run.

A task requires at least one parameter and one output mapping. Compose
the records first, then add the task to a project with \code{\link[=addPITask]{addPITask()}} and
run every task on the project with \code{\link[=runPI]{runPI()}}. To grow a task after it is
added, use \code{\link[=addPIParameter]{addPIParameter()}} / \code{\link[=addPIOutputMapping]{addPIOutputMapping()}}.
}
\examples{
PITask(
  id = "aciclovir_fit",
  scenarios = "aciclovir_iv",
  parameters = list(PIParameter(
    id = "lipophilicity",
    scenarios = "aciclovir_iv",
    path = "Aciclovir|Lipophilicity",
    minValue = -2,
    maxValue = 2,
    startValue = 0
  )),
  outputMappings = list(PIOutputMapping(
    id = "pvb",
    scenarios = "aciclovir_iv",
    outputPath = "aciclovir_pvb",
    observedData = "Laskin 1982.Group A"
  ))
)
}
\seealso{
\code{\link[=PIParameter]{PIParameter()}}, \code{\link[=PIOutputMapping]{PIOutputMapping()}}, \code{\link[=addPITask]{addPITask()}}, \code{\link[=runPI]{runPI()}}.

Other parameterIdentification:
\code{\link[=PIOutputMapping]{PIOutputMapping()}},
\code{\link[=PIParameter]{PIParameter()}},
\code{\link[=addPIOutputMapping]{addPIOutputMapping()}},
\code{\link[=addPIParameter]{addPIParameter()}},
\code{\link[=addPITask]{addPITask()}},
\code{\link[=removePIOutputMapping]{removePIOutputMapping()}},
\code{\link[=removePIParameter]{removePIParameter()}},
\code{\link[=removePITask]{removePITask()}}
}
\concept{parameterIdentification}
