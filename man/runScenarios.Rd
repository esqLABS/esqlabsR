% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scenario-execution.R
\name{runScenarios}
\alias{runScenarios}
\title{Run a set of scenarios.}
\usage{
runScenarios(
  project,
  scenarioNames = NULL,
  customParams = NULL,
  simulationRunOptions = NULL,
  validate = TRUE
)
}
\arguments{
\item{project}{An object of type \code{Project} loaded
from JSON. Its \code{scenarios} field is used to select and run scenarios.}

\item{scenarioNames}{Optional character vector of scenario names to run. If
\code{NULL} (default), all scenarios defined in \code{project} are run.}

\item{customParams}{A list containing vectors \code{paths}, \code{values}, and \code{units}
that will be applied to all scenarios.}

\item{simulationRunOptions}{Object of type \code{SimulationRunOptions} that will
be passed to simulation runs. If \code{NULL}, default options are used.}

\item{validate}{If \code{TRUE} (default), the \code{scenarios} and \code{crossReferences}
sections of the project are validated before running. Any critical
errors abort the run with a formatted message. Set to \code{FALSE} to skip
the pre-flight check (e.g. when intentionally running a partially
defined project). Validation is skipped automatically if a full
\code{\link[=validateProject]{validateProject()}} has already succeeded since the last project
mutation.}
}
\value{
A named list, where the names are scenario names, and the values are
lists with the entries \code{simulation} being the initialized \code{Simulation}
object with applied parameters, \code{results} being \code{SimulationResults} object
produced by running the simulation, \code{outputValues} the output values of the
\code{SimulationResults}, and \code{population} the \code{Population} object if the
scenario is a population simulation.
}
\description{
Run a set of scenarios.
}
\details{
If simulation of a scenario fails, a warning is produced, and the
\code{outputValues} for this scenario is \code{NULL}.
}
