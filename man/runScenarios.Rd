% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scenarios.R
\name{runScenarios}
\alias{runScenarios}
\title{Run a set of scenarios from a \code{Project}.}
\usage{
runScenarios(
  project,
  scenarios = NULL,
  customParams = NULL,
  simulationRunOptions = NULL,
  validate = TRUE,
  stopIfParameterNotFound = TRUE,
  stopIfFails = TRUE
)
}
\arguments{
\item{project}{A \link{Project} object loaded from a \code{Project.json} file.}

\item{scenarios}{Optional character vector of scenario names to
run. \code{NULL} (default) runs all scenarios in the project. Each name is
canonicalized (the same lowercasing and character-substitution ids get)
and matched against the canonical ids scenarios were authored under, so
the name you passed to \code{\link[=addScenario]{addScenario()}} resolves; a name that canonicalizes
to a different form warns, naming the id it resolved to.}

\item{customParams}{A list with vectors \code{paths}, \code{values}, and
\code{units} — applied to every selected scenario as the final
parameter layer.}

\item{simulationRunOptions}{Optional \link[ospsuite:SimulationRunOptions]{ospsuite::SimulationRunOptions}
for the simulation run. \code{NULL} (default) falls back to the project's
\code{defaultSimulationRunOptions}, including its solver setting
\code{checkForNegativeValues}, which is written to each simulation. An
explicit value replaces the project default entirely, so the
simulations keep the solver settings of their model files.}

\item{validate}{Logical. If \code{TRUE} (default), runs the relevant
section validators via \code{\link[=validateProject]{validateProject()}} before simulating and
aborts with a formatted summary on critical errors. Set to
\code{FALSE} to skip the pre-flight check (e.g. when the caller has
already validated the project).}

\item{stopIfParameterNotFound}{Logical. If \code{TRUE} (default), a
\code{customParams} path that matches no parameter in a scenario's
simulation aborts the run. Set to \code{FALSE} to skip such paths with a
warning instead.}

\item{stopIfFails}{Logical. If \code{TRUE} (default), a scenario that fails to
build (e.g. a missing model parameter path) or whose simulation produced
no results aborts the run with an error naming that scenario. Set to \code{FALSE}
to instead warn and leave that scenario's \code{results} and \code{outputValues}
\code{NULL} while the other scenarios are still built, run, and returned.}
}
\value{
A named list keyed by scenario name. Each entry is a list
with \code{simulation} (the initialized \link[ospsuite:Simulation]{ospsuite::Simulation}),
\code{results} (\link[ospsuite:SimulationResults]{ospsuite::SimulationResults}), \code{outputValues} (the
computed output values), and \code{population} (an \link[ospsuite:Population]{ospsuite::Population} for
population scenarios, or \code{NULL} for individual scenarios).

Every requested scenario gets an entry, including one that produced no
results under \code{stopIfFails = FALSE}. Such an entry carries \code{results = NULL}
(and \code{outputValues = NULL}), which is what tells a skipped scenario from one
that ran; a closing warning also names every scenario that produced none.
Select the ones that ran with
\verb{Filter(\\(x) !is.null(x$results), results)} before reaching into them.
}
\description{
Loads simulations, applies parameters, runs the
simulations, and collects results for one or more scenarios
defined on a parsed \link{Project}. The project must already have been
loaded with \code{\link[=loadProject]{loadProject()}}.
}
\details{
If a scenario fails, either at build time or because its
simulation produced no results, \code{runScenarios()} aborts by default
(\code{stopIfFails = TRUE}), naming the scenario. Set \code{stopIfFails = FALSE} to
instead produce a warning, skip the failing scenario, and leave its
\code{results} and \code{outputValues} \code{NULL}.
}
\seealso{
\code{\link[=buildSimulations]{buildSimulations()}} to obtain the parameterized simulations
without running them.
}
