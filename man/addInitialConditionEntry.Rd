% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parameters.R
\name{addInitialConditionEntry}
\alias{addInitialConditionEntry}
\title{Add one or many entries to a named initial-condition set}
\usage{
addInitialConditionEntry(project, id, path, value, unit)
}
\arguments{
\item{project}{A \code{Project} object.}

\item{id}{Character scalar, set id. Canonicalized; created if not present.}

\item{path}{Character vector of molecule paths (length N).}

\item{value}{Numeric vector of start values (length N).}

\item{unit}{Character vector of units (length N). A unit is mandatory for a
molecule start value; a blank unit is rejected.}
}
\value{
The \code{project} object, invisibly.
}
\description{
Adds molecule start-value entries to the named set in
\code{initialConditions} definitions. \code{path}, \code{value}, and \code{unit} accept parallel
vectors of equal length N to add all N entries in a single call (and a
single write to disk); a scalar call (length-1 vectors) adds one entry.
Building a large set with one vectorized call is far cheaper than a loop of
scalar calls, since each call rewrites the whole set file.
}
\details{
Unlike the other \verb{add*} functions, which abort on a missing parent, this
creates the parent set on demand if it does not yet exist (informing you
when it does). Last-write-wins on a duplicate \code{path}, including duplicates
within a single vectorized call.
}
\seealso{
Other parameters:
\code{\link[=addInitialConditions]{addInitialConditions()}},
\code{\link[=addParameterEntry]{addParameterEntry()}},
\code{\link[=addParameterSet]{addParameterSet()}},
\code{\link[=readInitialConditionsFromXLS]{readInitialConditionsFromXLS()}},
\code{\link[=removeInitialConditionEntry]{removeInitialConditionEntry()}},
\code{\link[=removeInitialConditions]{removeInitialConditions()}},
\code{\link[=removeParameterEntry]{removeParameterEntry()}},
\code{\link[=removeParameterSet]{removeParameterSet()}}
}
\concept{parameters}
