% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scenarios.R
\name{buildSimulations}
\alias{buildSimulations}
\title{Build the simulations for a set of scenarios without running them}
\usage{
buildSimulations(
  project,
  scenarios = NULL,
  customParams = NULL,
  simulationRunOptions = NULL,
  validate = TRUE,
  stopIfParameterNotFound = TRUE
)
}
\arguments{
\item{project}{A \link{Project} object loaded from a \code{Project.json} file.}

\item{scenarios}{Optional character vector of scenario names to
build. \code{NULL} (default) builds all scenarios in the project. Each name is
canonicalized (the same lowercasing and character-substitution ids get)
and matched against the canonical ids scenarios were authored under, so
the name you passed to \code{\link[=addScenario]{addScenario()}} resolves; a name that canonicalizes
to a different form warns, naming the id it resolved to.}

\item{customParams}{A list with vectors \code{paths}, \code{values}, and
\code{units} — applied to every selected scenario as the final
parameter layer.}

\item{simulationRunOptions}{Optional \link[ospsuite:SimulationRunOptions]{ospsuite::SimulationRunOptions}.
Consulted only for a scenario with \code{simulateSteadyState} set (the
steady-state pre-solve still runs); it is not applied to the returned
simulations, since they are not run here. \code{NULL} (default) falls back
to the project's \code{defaultSimulationRunOptions}, including its solver
setting \code{checkForNegativeValues}, which is written to each returned
simulation; an explicit value replaces the project default entirely,
so the simulations keep the solver settings of their model files.}

\item{validate}{Logical. If \code{TRUE} (default), runs the relevant
section validators via \code{\link[=validateProject]{validateProject()}} before building and
aborts with a formatted summary on critical errors. Set to
\code{FALSE} to skip the pre-flight check.}

\item{stopIfParameterNotFound}{Logical. If \code{TRUE} (default), a
\code{customParams} path that matches no parameter in a scenario's
simulation aborts the build. Set to \code{FALSE} to skip such paths with
a warning instead.}
}
\value{
A named list keyed by scenario name. Each entry is a list
with \code{simulation} (the initialized, not-yet-run
\link[ospsuite:Simulation]{ospsuite::Simulation}) and \code{population} (an \link[ospsuite:Population]{ospsuite::Population}
for population scenarios, or \code{NULL} for individual scenarios). Pass
the result to \code{\link[ospsuite:runSimulations]{ospsuite::runSimulations()}}, or inspect and edit the
\code{Simulation} first. This is not the shape \code{\link[=saveScenarioResults]{saveScenarioResults()}}
expects; use \code{\link[=runScenarios]{runScenarios()}} for that.
}
\description{
Loads and fully parameterizes (but does not run) the
\link[ospsuite:Simulation]{ospsuite::Simulation} (and, for a population scenario, the
\link[ospsuite:Population]{ospsuite::Population}) for one or more scenarios defined on a parsed
\link{Project}. Use this to inspect or modify a simulation before running
it yourself, to save the configured simulation to PKML, or to hand it
to another OSP-suite routine. To simulate and collect results in one
step, use \code{\link[=runScenarios]{runScenarios()}}.
}
\seealso{
\code{\link[=runScenarios]{runScenarios()}} to build and run in one step.
}
