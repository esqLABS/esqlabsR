% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parameter-identification.R
\name{PIParameter}
\alias{PIParameter}
\title{Create a Parameter Identification parameter}
\usage{
PIParameter(id, scenarios, path, units = NULL, minValue, maxValue, startValue)
}
\arguments{
\item{id}{Character scalar. Identifier for this parameter, unique within
its task. Used as a free label by the PI run, not as an entity-file id.}

\item{scenarios}{Character vector of scenario ids the parameter is
estimated across. Listing several scenarios fits one shared value
across all of them. The constructor does not check these against the
task's own \code{scenarios}; any that are not a subset of the task's
scenarios are reported later, by \code{\link[=validateProject]{validateProject()}} and at run time.}

\item{path}{Character scalar. Full simulation path of the model
parameter to estimate (OSPS notation).}

\item{units}{Optional character scalar. Display unit the bounds and
start value are expressed in. \code{NULL} or \code{""} means the model default
unit.}

\item{minValue, maxValue, startValue}{Numeric scalars. Search bounds and
the starting value; \verb{minValue <= startValue <= maxValue} must hold.}
}
\value{
A \code{PIParameter} object: a named list with copy semantics.
}
\description{
Builds a plain-data \code{PIParameter} record describing one
optimisation variable of a Parameter Identification task: the model
parameter to estimate, the scenarios it is fitted across, and its
search bounds and start value.

A \code{PIParameter} is a building block for a \code{\link[=PITask]{PITask()}}; pass a list of
them as the \code{parameters} argument of \code{\link[=PITask]{PITask()}} or \code{\link[=addPITask]{addPITask()}}, or
add one to an existing task with \code{\link[=addPIParameter]{addPIParameter()}}.
}
\examples{
PIParameter(
  id = "lipophilicity",
  scenarios = "aciclovir_iv",
  path = "Aciclovir|Lipophilicity",
  minValue = -2,
  maxValue = 2,
  startValue = 0
)
}
\seealso{
\code{\link[=PITask]{PITask()}}, \code{\link[=PIOutputMapping]{PIOutputMapping()}}, \code{\link[=addPIParameter]{addPIParameter()}}.

Other parameterIdentification:
\code{\link[=PIOutputMapping]{PIOutputMapping()}},
\code{\link[=PITask]{PITask()}},
\code{\link[=addPIOutputMapping]{addPIOutputMapping()}},
\code{\link[=addPIParameter]{addPIParameter()}},
\code{\link[=addPITask]{addPITask()}},
\code{\link[=removePIOutputMapping]{removePIOutputMapping()}},
\code{\link[=removePIParameter]{removePIParameter()}},
\code{\link[=removePITask]{removePITask()}}
}
\concept{parameterIdentification}
