% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parameter-identification.R
\name{setPITask}
\alias{setPITask}
\title{Modify a Parameter Identification task's task-level fields}
\usage{
setPITask(project, id, scenarios, configuration)
}
\arguments{
\item{project}{A \code{Project} object.}

\item{id}{Character scalar. Id of the task to modify, canonicalized the same
way \code{\link[=addPITask]{addPITask()}} canonicalizes it. The task must already exist.}

\item{scenarios}{Character vector of scenario ids the task runs against. Each
must exist in \code{names(project$definitions$scenarios)}.}

\item{configuration}{Named list of solver settings; see the \code{configuration}
argument of \code{\link[=PITask]{PITask()}} for the supported keys. \code{NULL} clears every setting,
leaving the runtime defaults in place.}
}
\value{
The \code{project} object, invisibly.
}
\description{
Changes the task-level fields of the task identified by \code{id}:
the \code{scenarios} it runs against and its solver \code{configuration}. Only the
arguments you pass are changed; the other keeps its current value (partial
update). Passing \code{scenarios = character(0)} or \code{configuration = NULL}
empties that field.

A task's parameters and output mappings are not task-level fields: grow and
shrink them with \code{\link[=addPIParameter]{addPIParameter()}} / \code{\link[=removePIParameter]{removePIParameter()}} and
\code{\link[=addPIOutputMapping]{addPIOutputMapping()}} / \code{\link[=removePIOutputMapping]{removePIOutputMapping()}}. To replace a whole task
at once, use \code{addPITask(..., overwrite = TRUE)}.

Like \code{\link[=addPITask]{addPITask()}}, this acts on one task per call.
}
\seealso{
Other parameterIdentification:
\code{\link[=PIOutputMapping]{PIOutputMapping()}},
\code{\link[=PIParameter]{PIParameter()}},
\code{\link[=PITask]{PITask()}},
\code{\link[=addPIOutputMapping]{addPIOutputMapping()}},
\code{\link[=addPIParameter]{addPIParameter()}},
\code{\link[=addPITask]{addPITask()}},
\code{\link[=removePIOutputMapping]{removePIOutputMapping()}},
\code{\link[=removePIParameter]{removePIParameter()}},
\code{\link[=removePITask]{removePITask()}}
}
\concept{parameterIdentification}
