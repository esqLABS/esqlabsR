% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scenarios.R
\name{Scenario}
\alias{Scenario}
\title{Create a Scenario}
\usage{
Scenario(
  scenarioName = NULL,
  modelFile = NULL,
  applicationProtocol = NULL,
  individualId = NULL,
  populationId = NULL,
  outputPaths = NULL,
  simulationType = if (is.null(populationId)) "Individual" else "Population",
  readPopulationFromCSV = FALSE,
  simulateSteadyState = FALSE,
  simulationTime = NULL,
  simulationTimeUnit = NULL,
  steadyStateTime = 1000,
  steadyStateTimeUnit = NULL,
  overwriteFormulasInSS = FALSE,
  modelParameterSets = NULL,
  initialConditions = NULL
)
}
\arguments{
\item{scenarioName}{Character. Name of the scenario.}

\item{modelFile}{Character. Name of the \code{.pkml} model file (relative to
the model folder).}

\item{applicationProtocol}{Character or \code{NA}. Name of the application
protocol; \code{NA} when absent.}

\item{individualId}{Character or \code{NULL}. ID referencing
\code{project$individuals}.}

\item{populationId}{Character or \code{NULL}. ID referencing
\code{project$populations}.}

\item{outputPaths}{Named character vector of literal output paths.
Names are the ids referencing \code{project$outputPaths}; values are the
literal paths. \code{NULL} when the scenario declares no outputs.
Round-trip serialization reads \code{names(outputPaths)} to rebuild the
\code{outputPaths} id array, so the named-vector invariant must be preserved.}

\item{simulationType}{Character. \code{"Individual"} or \code{"Population"}.
Defaults to \code{"Population"} when \code{populationId} is set,
\code{"Individual"} otherwise.}

\item{readPopulationFromCSV}{Logical. If \code{TRUE}, load population from
CSV.}

\item{simulateSteadyState}{Logical. If \code{TRUE}, run steady-state before
the main simulation.}

\item{simulationTime}{List of length-3 numeric vectors
\code{c(start, end, resolution)}.}

\item{simulationTimeUnit}{Character. Time unit for \code{simulationTime}.}

\item{steadyStateTime}{Numeric. Steady-state time \strong{in base unit
(minutes)}.}

\item{steadyStateTimeUnit}{Character. Original unit for
\code{steadyStateTime}, preserved for round-trip serialization.}

\item{overwriteFormulasInSS}{Logical. Overwrite formula parameters
during steady-state.}

\item{modelParameterSets}{Character vector. Parameter-set ids
referencing \code{project$parameterSets}.}

\item{initialConditions}{Character vector. Initial-condition set ids
referencing \code{project$initialConditions}.}
}
\value{
A \code{Scenario} object: a named list carrying exactly the fields
above.
}
\description{
Builds a plain-data \code{Scenario} record holding the
configuration fields of a v2.0 \code{Project.json} scenario entry. It does
not create or hold ospsuite runtime objects; the runtime is built by
\code{\link[=runScenarios]{runScenarios()}} at execution time.

A \code{Scenario} is a named list with copy semantics: an entry extracted
from \code{project$scenarios} is an independent copy. The section accessor is
read-only, so to apply a change you pass the record to an authoring
function (\code{addScenario()} / \code{setScenario()}), which validates and writes
it through to the project.
}
