% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scenarios.R
\name{duplicateScenario}
\alias{duplicateScenario}
\title{Duplicate an existing scenario}
\usage{
duplicateScenario(project, id, newId)
}
\arguments{
\item{project}{A \code{Project} object.}

\item{id}{Character. Id of the scenario to copy; must exist in
\code{scenarios} definitions (after canonicalization).}

\item{newId}{Character. Id for the new copy; its canonical form must not
already belong to an existing scenario.}
}
\value{
The \code{project} object, invisibly.
}
\description{
Creates a deep copy of the scenario currently keyed \code{id} under
\code{newId}, leaving the original untouched. The copy is a new definition
written through to \code{newId} (the in-memory store and the on-disk project
both gain an independent scenario).

Both \code{id} and \code{newId} are canonicalized the same way \code{\link[=addScenario]{addScenario()}}
canonicalizes an id (lowercased, made a safe single-path-segment id, with
a warning when the value changed).
}
\seealso{
Other scenario:
\code{\link[=addScenario]{addScenario()}},
\code{\link[=removeScenario]{removeScenario()}},
\code{\link[=renameScenario]{renameScenario()}},
\code{\link[=setScenario]{setScenario()}}
}
\concept{scenario}
