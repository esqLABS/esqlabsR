% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/definition-files.R
\name{loadSnapshot}
\alias{loadSnapshot}
\title{Load a Project from a single-file snapshot, materializing its tree}
\usage{
loadSnapshot(file, dir)
}
\arguments{
\item{file}{Path to the snapshot file to read (a \code{.esqlabsR} file, or a
plain inlined \code{Project.json}). Required; a snapshot path is never
optional.}

\item{dir}{Target directory for the materialized tree project. Required.
It is created if it does not exist. If it already contains an esqlabsR
project, \code{loadSnapshot()} aborts rather than overwrite it; pass an empty
or new directory.}
}
\value{
Object of type \code{Project}, bound to \code{dir}.
}
\description{
Reads a single self-contained snapshot file (a portable
freeze-frame with every section inlined) and writes it back out as a full
on-disk tree project at \code{dir}: a \code{Project.json} container plus a
\verb{definitions/<kind>/} tree (one file per definition) for every section.
Loading a snapshot \emph{is} materializing it; there is no separate materialize
step. The returned \code{Project} is bound to \code{dir}, so further \verb{add*} /
\verb{remove*} / \verb{set*} edits are write-through to the new tree.

The canonical snapshot form is a \code{.esqlabsR} file (as written by
\code{\link[=saveSnapshot]{saveSnapshot()}}), but a plain inlined \code{Project.json} is also accepted for
back-compatibility (for example, the file \code{\link[=importProjectFromExcel]{importProjectFromExcel()}}
writes). The result is a normal tree project: \code{\link[=loadProject]{loadProject()}} reads it back
from \code{dir} identically (section for section).
}
\examples{
# Write a snapshot, then materialize it into a fresh tree project.
src <- file.path(tempdir(), "loadsnapshot-src")
dir.create(src, showWarnings = FALSE)
initProject(src, type = "example", createExcel = FALSE)
snapshot <- saveSnapshot(
  loadProject(file.path(src, "Project.json")),
  file.path(tempdir(), "shared")
)
project <- loadSnapshot(snapshot, file.path(tempdir(), "restored"))
}
\seealso{
\code{\link[=saveSnapshot]{saveSnapshot()}}, \code{\link[=loadProject]{loadProject()}}.

Other project persistence:
\code{\link[=exampleProjectPath]{exampleProjectPath()}},
\code{\link[=exportProjectToExcel]{exportProjectToExcel()}},
\code{\link[=importProjectFromExcel]{importProjectFromExcel()}},
\code{\link[=initProject]{initProject()}},
\code{\link[=loadProject]{loadProject()}},
\code{\link[=saveSnapshot]{saveSnapshot()}}
}
\concept{project persistence}
