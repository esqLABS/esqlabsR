% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation.R
\name{print.ValidationResults}
\alias{print.ValidationResults}
\title{Print a project validation report}
\usage{
\method{print}{ValidationResults}(x, ...)
}
\arguments{
\item{x}{A \code{ValidationResults} object, the value of \code{\link[=validateProject]{validateProject()}}.}

\item{...}{Ignored, present for S3 compatibility.}
}
\value{
\code{x}, invisibly.
}
\description{
Renders the named list of per-section \code{validationResult} objects that
\code{\link[=validateProject]{validateProject()}} returns into a human-readable summary, grouped by
definition type (the list keys: \code{scenarios}, \code{individuals}, and the
rest). The structured object itself is unchanged and stays indexable
(\code{results$scenarios$critical_errors}); only the console view differs.
}
\details{
The summary opens with overall counts (the same aggregation as
\code{\link[=validationSummary]{validationSummary()}}), then lists each definition type that has at
least one issue: a cross marks each critical error, a \code{!} marks each
warning, and the \code{category} of each entry is shown as a sub-label.
Definition types with no issues are folded into a compact "N
section{?s} OK" tail. A fully valid result prints a single "no issues"
line. Glyphs and styling come from \code{cli}, so the output degrades
gracefully to plain ASCII when unicode or colour is unavailable.
}
\seealso{
\code{\link[=validateProject]{validateProject()}}, \code{\link[=validationSummary]{validationSummary()}},
\code{\link[=isAnyCriticalErrors]{isAnyCriticalErrors()}}.
}
