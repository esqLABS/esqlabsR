% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/entity-files.R
\name{saveSnapshot}
\alias{saveSnapshot}
\title{Write a derived single-file Project snapshot}
\usage{
saveSnapshot(project, path = NULL)
}
\arguments{
\item{project}{A \code{Project} object.}

\item{path}{Path where the snapshot should be written. The output is
normalized to a \code{.esqlabsR} file: a path with no extension or a \code{.json}
extension is written as \code{.esqlabsR} (e.g. \code{"study1"} and \code{"study1.json"}
both write \code{study1.esqlabsR}); a \code{.esqlabsR} path is used verbatim; any
other explicit extension is honored as given (with an informational note
that \code{.esqlabsR} is the canonical form). Must resolve to a location other
than the project's own container (\code{project$jsonPath}): a snapshot is a
derived artifact, so writing it onto the authoritative tree's container
would inline sections that the \verb{definitions/} tree already owns and the
two would diverge on reload. For an in-memory project (no \code{jsonPath}),
\code{path} is required.}
}
\value{
Invisibly returns the (normalized) path the snapshot was written
to.
}
\description{
Renders a \code{Project} to a single self-contained \code{.esqlabsR}
snapshot file with every section inlined. The content is JSON; the
\code{.esqlabsR} extension marks the file as a portable shareable freeze-frame,
distinguishing it at a glance from the \code{Project.json} container of a live
tree project. The snapshot is derived; the authoritative form remains the
definitions tree (the \verb{definitions/} directory next to the project file).
Reloading a snapshot with \code{\link[=loadSnapshot]{loadSnapshot()}} writes the project back out as
an on-disk \verb{definitions/} tree (loading a snapshot materializes it) and
yields a \code{Project} structurally identical to the source, so snapshot then
load then snapshot is a fixed point.

Authoring is write-through: every \verb{add*} / \verb{remove*} / \verb{set*} edit lands
in its definition file immediately, so \code{saveSnapshot()} is not needed to
persist edits. It produces the single-file shareable freeze-frame.
}
\examples{
# Scaffold a throwaway example project and snapshot it to a single file.
dir <- file.path(tempdir(), "snapshot-example")
dir.create(dir, showWarnings = FALSE)
initProject(dir, type = "example", createExcel = FALSE)
project <- loadProject(file.path(dir, "Project.json"))
snapshot <- saveSnapshot(project, file.path(tempdir(), "study"))
snapshot # the normalized .esqlabsR path
}
\seealso{
\code{\link[=loadSnapshot]{loadSnapshot()}}, \code{\link[=loadProject]{loadProject()}}.

Other project persistence:
\code{\link[=exampleProjectPath]{exampleProjectPath()}},
\code{\link[=exportProjectToExcel]{exportProjectToExcel()}},
\code{\link[=importProjectFromExcel]{importProjectFromExcel()}},
\code{\link[=initProject]{initProject()}},
\code{\link[=loadProject]{loadProject()}},
\code{\link[=loadSnapshot]{loadSnapshot()}}
}
\concept{project persistence}
